<Page x:Class="CalendarList.SelectPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CalendarList"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="SelectPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="5*"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <ListBox x:Name="SelectList" Grid.ColumnSpan="2"></ListBox>
        <Button Content="Сохранить" x:Name="Save" Grid.Row="1" Height="NaN" Click="Save_Click"></Button>
        <Button Content="Не сохранять" x:Name="NoSave" Grid.Row="1" Height="NaN" Click="NoSave_Click" Grid.Column="1"></Button>
    </Grid>
</Page>
